# DISABLED — Sprint 21: CI/CD migrated to VPS webhook server (nammon.men/hooks/).
# Push to main now triggers the self-hosted webhook at https://nammon.men/hooks/deploy
# which runs /root/.../webhook/deploy-about.sh on the VPS.
# This workflow is kept for reference only. Do not re-enable push trigger.

name: Deploy to GitHub Pages (DISABLED)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is disabled. CI/CD is handled by the VPS webhook server."
